{
  "name": "refinerymap-cdu-mass-balance",
  "version": "1.0.0",
  "private": true,
  "description": "RefineryMap CDU Mass Balance with read-only Google Gemini analysis",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.recovery.v8.ai.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.21.2"
  }
}
